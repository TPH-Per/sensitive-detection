project:
  name: video_moderation_v5_2
  seed: 42
  deterministic: true

runtime:
  device: cuda
  mixed_precision: fp16
  num_workers: 4
  pin_memory: true
  log_every_n_steps: 20

paths:
  data_root: /kaggle/input/your-dataset
  output_root: /kaggle/working/artifacts

logging:
  use_tensorboard: true
  save_csv_metrics: true
